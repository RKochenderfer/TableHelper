networks:
  tablehelper_net:
services:
  tablehelper.api:
    image: ghcr.io/rkochenderfer/tablehelper:latest
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - '8080:8080'
    restart: always
    environment:
      - SQLITE_DB_PATH=SQLITE_DB_PATH
    volumes:
      - SQLITE_DB_PATH:./data

